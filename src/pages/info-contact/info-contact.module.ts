import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { InfoContactPage } from './info-contact';

@NgModule({
  declarations: [
    InfoContactPage,
  ],
  imports: [
    IonicPageModule.forChild(InfoContactPage),
  ],
})
export class InfoContactPageModule {}
